# Stream of Characters

Design an algorithm that accepts a stream of characters and checks if a suffix of these characters is a string of a given array of strings words.

For example, if words = ["abc", "xyz"] and the stream added the four characters (one by one) 'a', 'x', 'y', and 'z', your algorithm should detect that the suffix "xyz" of the characters "axyz" matches "xyz" from words.

Implement the StreamChecker class:

StreamChecker(String[] words) Initializes the object with the strings array words.
boolean query(char letter) Accepts a new character from the stream and returns true if any non-empty suffix from the stream forms a word that is in words.
 

Example 1:

Input
["StreamChecker", "query", "query", "query", "query", "query", "query", "query", "query", "query", "query", "query", "query"]
[[["cd", "f", "kl"]], ["a"], ["b"], ["c"], ["d"], ["e"], ["f"], ["g"], ["h"], ["i"], ["j"], ["k"], ["l"]]
Output
[null, false, false, false, true, false, true, false, false, false, false, false, true]

Explanation
StreamChecker streamChecker = new StreamChecker(["cd", "f", "kl"]);
streamChecker.query("a"); // return False
streamChecker.query("b"); // return False
streamChecker.query("c"); // return False
streamChecker.query("d"); // return True, because 'cd' is in the wordlist
streamChecker.query("e"); // return False
streamChecker.query("f"); // return True, because 'f' is in the wordlist
streamChecker.query("g"); // return False
streamChecker.query("h"); // return False
streamChecker.query("i"); // return False
streamChecker.query("j"); // return False
streamChecker.query("k"); // return False
streamChecker.query("l"); // return True, because 'kl' is in the wordlist

### Solution 
它的难点在于：输入不是一次性给出的，而是一个字符一个字符“流”进来的。每次进来一个新字符，你都要判断：以当前字符结尾的任何后缀，是否在给定的字典里？
1. 核心挑战：正着找还是倒着找？
假设字典里有 ["abcd", "efg"]，流进来的字符序列是 ... a, b, c, d。

如果你正向存储（a -> b -> c -> d）：
当 d 进来时，你需要检查以 d 结尾的组合：d, cd, bcd, abcd。这需要你在 Trie 里从不同起点出发，效率极低。

如果你反向存储（d -> c -> b -> a）：
当你把流进来的字符也反向看，问题就变成了：从最新的字符开始，往回找，看是否匹配 Trie 里的某个前缀。

2. 算法思路：倒序 Trie
第一步：建立反向 Trie
把单词表里的每个单词翻转后插入 Trie。
例如：单词 "apple" 在 Trie 里存为 e -> l -> p -> p -> a。

第二步：维护字符流（单调容器）
用一个 deque 或 list 记录流进来的字符。为了防止列表无限增长，它的长度只需要保持在字典里最长单词的长度即可。

第三步：查询 (Query)
每当一个新字符 char 进来：

将它放入流容器的最前端（或者保持流，从后往前遍历）。

从 Trie 的根节点出发，顺着流里的字符一个一个往下找。

如果在找的过程中遇到了一个单词标记位（isEnd = true），说明找到了一个匹配的后缀，返回 True。

如果走到一半断了，或者流看完了还没遇到标记位，返回 False。
```python
class TrieNode:
    def __init__(self):
        self.end = False
        self.children = [None] * 26
class Trie:
    def __init__(self):
        self.root = TrieNode()
    def add(self, word):
        cur = self.root
        i = len(word)-1
        while i >= 0:
            idx = ord(word[i]) - ord('a')
            if not cur.children[idx]:
                cur.children[idx] = TrieNode()
            cur = cur.children[idx]
            if i == 0:
                cur.end = True
            i -= 1
    def search(self, wordLst):
        cur = self.root
        for i in range(len(wordLst)-1, -1, -1):
            idx = ord(wordLst[i]) - ord('a')
            if not cur.children[idx]:
                return False
            cur = cur.children[idx]
            if cur.end:
                return True
        return False
class StreamChecker:
    def __init__(self, words: List[str]):
        self.trie = Trie()
        self.maxLen = 0
        for w in words:
            self.trie.add(w)
            self.maxLen = max(self.maxLen, len(w))
        self.stream = []
    def query(self, letter: str) -> bool:
        self.stream.append(letter)
        if len(self.stream) > self.maxLen:
            self.stream.pop(0)
        return self.trie.search(self.stream)


# Your StreamChecker object will be instantiated and called as such:
# obj = StreamChecker(words)
# param_1 = obj.query(letter)
```