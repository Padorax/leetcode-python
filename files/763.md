# Partition Labels
You are given a string s. We want to partition the string into as many parts as possible so that each letter appears in at most one part. For example, the string "ababcc" can be partitioned into ["abab", "cc"], but partitions such as ["aba", "bcc"] or ["ab", "ab", "cc"] are invalid.

Note that the partition is done so that after concatenating all the parts in order, the resultant string should be s.

Return a list of integers representing the size of these parts.

 
Example 1:
Input: s = "ababcbacadefegdehijhklij"
Output: [9,7,8]
Explanation:
The partition is "ababcbaca", "defegde", "hijhklij".
This is a partition so that each letter appears in at most one part.
A partition like "ababcbacadefegde", "hijhklij" is incorrect, because it splits s into less parts.

Example 2:
Input: s = "eccbbbbdec"
Output: [10]

### Solution
Greedy
我们先用数组或哈希表last记录字符串s中每个字母最后一次出现的位置。

接下来我们使用贪心的方法，将字符串划分为尽可能多的片段。从左到右遍历字符串s, 遍历的同时维护当前片段的开始下标j 和结束下标i 初始均为0
对于每个访问到的字母c 获取到最后一次出现的位置last[c]。由于当前片段的结束下标一定不会小于last[c], 因此令 mx=max(mx, last[c])

当访问到下标mx时，意味着当前片段访问结束，当前片段的下标范围是[j,,,i], 长度为i - j + 1，我们将其添加到结果数组中。然后令j=i+1, 继续寻找下一个片段。

重复上述过程，直至字符串遍历结束，即可得到所有片段的长度
```python
class Solution:
    def partitionLabels(self, s: str) -> List[int]:
        last = {c: i for i, c in enumerate(s)}
        end = start = 0
        ans = []
        for i, c in enumerate(s):
            end = max(end, last[c])
            if end == i:
                ans.append(i - start + 1)
                start = i + 1
                # no need to update end here
        return ans
```
or
```python
from collections import defaultdict
class Solution:
    def partitionLabels(self, s: str) -> List[int]:
        occurMap = defaultdict(list)
        for i, c in enumerate(s):
            occurMap[c].append(i)
        res = []

        start = 0
        while start < len(s):
            end = occurMap[s[start]][-1]
            i = start
            # keep increasing right till it meets end
            while i < end:
                i += 1
                end = max(end, occurMap[s[i]][-1])
            res.append(end-start+1)
            start = end + 1
        return res
```

if we don't need (start, end) of each label group but just size:
```python
class Solution:
    def partitionLabels(self, s: str) -> List[int]:
        lastIndex = {}
        for i, c in enumerate(s):
            lastIndex[c] = i

        res = []
        size = end = 0
        for i, c in enumerate(s):
            size += 1
            end = max(end, lastIndex[c])

            if i == end:
                res.append(size)
                size = 0
        return res
```

note failure case:
```python
class Solution:
    def partitionLabels(self, s: str) -> List[int]:
        last = {c: i for i, c in enumerate(s)}
        start = 0
        end = last[s[0]]
        ans = []
        for i, c in enumerate(s):
            if end == i:
                ans.append(i - start + 1)
                start = i + 1
                # need to init end for new group

            end = max(end, last[c])
        return ans
```
"eaaaabaaec", expected[9, 1], but will get[9] as when we reach "c", end is not updated yet