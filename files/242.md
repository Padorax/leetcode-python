```python
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if not s or not t: return s == t
        if len(s) != len(t): return False
        return self.sortLetter(s) == self.sortLetter(t)

    def sortLetter(self, s: str) -> str:
        return ''.join(sorted(s))
```